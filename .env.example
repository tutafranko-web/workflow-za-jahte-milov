# Copy to .env and fill values. .env is gitignored.

# Serper.dev — Google search API (reused from Search and Scraper workflow)
SERPER_API_KEY=74e6ddba7e5a3056303532712157698afbce42c8

# Firecrawl — web scraping (needed from user; was stored as n8n credential)
FIRECRAWL_API_KEY=

# Optional: SMTP fallback (only used if --smtp flag is passed)
# Generate at: myaccount.google.com → Security → 2FA → App passwords
GMAIL_APP_PASSWORD=
